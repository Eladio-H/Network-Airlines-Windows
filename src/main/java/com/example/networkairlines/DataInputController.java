package com.example.networkairlines;

import javafx.fxml.Initializable;
import java.net.URL;
import java.util.ResourceBundle;

public class DataInputController extends SettingsController implements Initializable {
    /**
     * This screen is pretty much the same as the Settings screen so the controller extends from it, but the initialising
     * function in this screen is unnecessary.
     * @param url
     * @param resourceBundle
     */
    @Override
    public void initialize(URL url, ResourceBundle resourceBundle){}
}